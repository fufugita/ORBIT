# ORBIT orbit-egress — Phase B implementation
# PURPOSE: Egress broker: EgressIntent fsync-before-network, DNS/TLS pinning (DR-09 GW-04, DR-06 §6.4)
# B6: egress broker with strict ordering: parse → allowlist → policy → lifecycle →
#     EgressEvent fsync → DNS → TLS → payload.

[package]
name = "orbit-egress"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Egress broker: EgressIntent fsync-before-network, DNS/TLS pinning"

[dependencies]
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
hex.workspace = true
thiserror.workspace = true
