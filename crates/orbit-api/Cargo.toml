# ORBIT orbit-api — Phase D implementation
# PURPOSE: HTTP/API surface for the six primitives (envelopes, tasks, memory, trust, evidence)
# Contract: DR-12 §4.5. D15. (HTTP server binding deferred; request/response shapes + handlers now.)

[package]
name = "orbit-api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "HTTP/API surface for the six primitives (envelopes, tasks, memory, trust, evidence)"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
